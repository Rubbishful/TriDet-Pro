scripts/full_pipeline.py	                                      📜 主流水线脚本	~24KB
scripts/pytorch_i3d.py	                                          I3D 模型定义（脚本引用）	
configs/id3_i3d.yaml	                                          TriDet 模型配置（20类, 2048-dim）	
epoch_039.pth.tar	                                              TriDet 训练权重（gitignored）	（训练得到）
pytorch-i3d-feature-extraction-master/models/rgb_imagenet.pt	  I3D RGB 预训练权重	(自行下载)
pytorch-i3d-feature-extraction-master/models/flow_imagenet.pt	  I3D Flow 预训练权重	(自行下载)
pytorch-i3d-feature-extraction-master/pytorch_i3d.py	          I3D 模型定义（已 A staged）	
data/id3_2048/annotations.json	                                  标签定义（20类 THUMOS14 标签）	
scripts/extract_features.py                                       单独启用id3特征提取

输入格式   直接由视频输出结果                                        以id3特征直接输出结果
scripts/full_pipeline.py 
--video 视频路径
--config configs/id3_i3d.yaml 
--ckpt epoch_039.pth.tar 
--output_dir 结果输出                                              结果会以txt形式输出
# 从原始视频提取 RGB 特征 (2048-dim)
    python scripts/extract_features.py \
    --video_path 视频路径
    --output_dir 输出路径
    --mode rgb+flow \
    --device cpu

# 从预提取帧提取 RGB+Flow 特征 (2048-dim)                           请注意，以帧形式输出id3特征时，至少需要16帧画面
python scripts/extract_features.py \
    --frames_dir 帧路径
    --output_dir 输出路径
    --mode rgb+flow \
    --device cpu
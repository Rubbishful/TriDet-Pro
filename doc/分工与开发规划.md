搜集资料和代码开发我们不分开，按任务主题来分工。
分工：
1. 测试和分析
2. 主体检测跟踪
3. 端到端特征
4. 重叠动作与多主体
5. 审核、合并、整体改进与机动支援
# 测试与分析
## 接口形式化
张量是什么？张量就是矩阵，我们知道矩阵的乘法对形状尺寸是有非常严格的要求的，差一点就报错。
原始代码几乎没有张量shape的文档，所以现在每个模块的输入输出我们都必须给出非常详尽的说明，形状、用法、意义、限制、数据在模块内的流向，都必须写得一清二楚。
```python
# 示例：SGPBackbone 的接口契约
def forward(self, x: Tensor, mask: Tensor) -> Tuple[Tuple[Tensor, …], Tuple[Tensor, …]]:
    """
    Args:
        x: (B, C_in, T) 输入特征
        mask: (B, 1, T) 有效位置的 bool mask
    Returns:
        feats: 长度为 1+arch[2] 的 tuple，第 i 个元素 shape 为 (B, C_embd, T // scale_factor^i)
        masks: 对应长度的 mask tuple
    """
```
这对后续改进至关重要——如果我们后面要替换一些模块的时候，只要满足相同的输入输出约定即可，非常快捷。
## 整体分析
分析不同因素（如动作时长、视频复杂度、特征分辨率等）对检测性能的影响——这是明确的课设要求。
所以必须调好能跑通模型的环境，数据可以到我这拷。
### 消融测试
消融测试（Ablation Study）指的是逐个移除或替换模型组件，测量每个组件对性能的贡献。对于 TriDet，消融测试的两大创新点（Trident-head 和 SGP 层）以及训练策略中的关键超参。消融实验的结果是答辩中最有说服力的数据——"我们证明了 X 模块贡献了 Y% 的 mAP"。

**消融实验设计**（按优先级排序，每项都需要独立训练+评估一轮）:

| 编号 | 消融项 | 修改方式 | 预期影响 | 训练时间 |
|------|--------|---------|---------|---------|
| A1 | Trident-head → 普通回归头 | `use_trident_head=False` | 边界精度下降，预计 -2~4% mAP | ~1h (THUMOS) |
| A2 | SGP Backbone → Conv Backbone | `backbone_type='conv'` | 特征判别力下降，预计 -5~7% mAP | ~1h |
| A3 | SGP 窗口尺寸消融 | `n_sgp_win_size=[1,3,5,7,9,11]` 逐组测试 | 找到最优窗口配置 | 每组 ~1h |
| A4 | SGP 的 k 参数消融 | `k=1.0, 1.5, 3.0, 5.0, 7.0` 分别训练 | 分析多尺度融合的敏感度 | 每组 ~1h |
| A5 | DIoU → GIoU 损失 | 替换 `losses.py` 中回归损失 | 评估边界回归损失函数的影响 | ~1h |
| A6 | FPN vs Identity Neck | `fpn_type='identity'` | 评估多尺度融合的作用，预计 -1~3% | ~1h |
| A7 | 中心采样半径消融 | `center_sample_radius=0.0, 0.5, 1.0, 1.5, 2.0` | 分析正样本分配策略的影响 | 每组 ~1h |
| A8 | 分类损失权重 | `loss_weight=0.5, 1.0, 2.0, 5.0` | 分析 cls/reg 损失平衡 | 每组 ~1h |

**消融实验执行规范**:
- 统一在 THUMOS14 上运行（训练快，~1h/轮），ActivityNet 只在最终方案上验证
- 每轮固定随机种子 (`fix_random_seed(2023)`)，保证可复现
- 记录结果到统一的 CSV: `ablation_results.csv`，格式: `实验编号, 配置变更, mAP@0.3, mAP@0.5, mAP@0.7, avg_mAP, 训练时间`
- A1-A2 是必须做的（验证核心创新），A3-A8 选做其中 3-4 项即可

**消融结果可视化**:
- 柱状图对比各消融项的 avg_mAP，标注与基线的差值
- 折线图展示 k 参数/窗口尺寸的 mAP 变化趋势，找最优值

### 其他分析任务
- **按动作时长分层分析**: 将 GT 实例按时长分为 短(<2s)、中(2-10s)、长(>10s) 三组，分别计算 recall，找出 TriDet 在哪个时长区间表现最差
- **按视频动作密度分层分析**: 统计每个视频的 GT 实例数，按密度分桶（1-3, 4-6, 7-10, >10），分桶计算 mAP
- **混淆矩阵分析**: 统计 THUMOS14 的 20 类混淆矩阵，找出高频误分类对（如 "PoleVault" ↔ "HighJump"），分析原因
## 其他可选任务
### 模块测试
可以尝试为单独的模块写测试脚本，为后续的分析提供底层数据的关键。
有这么一些测试可以写：

| 层级       | 测什么                | 方法                                                  |
| -------- | ------------------ | --------------------------------------------------- |
| **单元测试** | 每个模块的输出 shape 是否正确 | 构造假输入 `(B=2, C=2048, T=128)`，跑 forward，assert shape |
| **数值测试** | 损失函数计算是否正确         | 构造已知输入输出，对比手算结果                                     |
| **回归测试** | 与上游仓库的输出是否一致       | 加载上游权重，喂相同输入，对比 logits 差异 < 1e-5                    |
| **集成测试** | 训练一步不报错            | dummy data + 1 step forward/backward                |

当然这些测试用的脚本、数据，请放在
### 分析设计决策
原始代码只展示了 HOW。复现的核心价值在于理解 WHY：
- 为什么 `center_sample = 'radius'`？→ 减少边界模糊样本对训练的干扰
- 为什么 `num_bins` 是 16？→ Trident head 将连续回归离散化为分类问题
- 为什么 `SGPBlock` 里有 `psi, fc, convw, convkw, global_fc` 五个分支？→ 分别建模 即时特征、全局上下文、局部窗口、大窗口、全局池化
- 为什么 `max_seq_len` 必须被 32 整除？→ FPN 的 stride 累积要求（这就是你 memory 里已记录的约束）
这些理解应该写成模块级的 docstring，而不是行内注释。

---

# 三个具体改进方向
- 搜集论文，一方面是为自己的改进工作作参考、打开思路，另一方面答辩的时候也能更有底气。
- 写代码，往自己的分支里提交，然后来合并到master。

## 主体人物检测与跟踪
**核心目标**: 在 TriDet 检测出动作片段后，自动关联执行该动作的人物主体（边界框 + 身份 ID），从"什么时候发生了什么动作"升级到"什么人在什么时候做了什么动作"。
**为什么值得做**:
- 这是时序动作检测 + 时空动作检测的交叉领域，答辩时很容易体现出技术深度
- 实际应用价值高（安防监控、体育分析、行为识别）
- 技术栈成熟：YOLO 检测 + 跟踪算法 都是开箱即用的模型，不需要自己标注和训练
- TriDet 原论文明确将此列为未来方向（Limitation 部分）
### 技术路线
**阶段 1: 基础关联（先跑通，1-2 天）**

使用 YOLOv8 + 简单的时间戳匹配：

```
视频帧 → YOLOv8n 逐帧检测人物 → 取动作时间窗口中间帧的检测框 → 输出关联结果
```

- 使用 `ultralytics` 库的 YOLOv8n 预训练模型（COCO person 类别=0），无需自己训练
- 检测频率: 不必每帧都检测，按视频原始 fps 每 0.5s 采样一帧即可
- 关联逻辑: 动作片段 `[t_start, t_end]` → 取 `(t_start + t_end) / 2` 时刻最近帧 → 取置信度最高的人物框
- 输出格式: 在 TriDet 的检测结果中附加 `subject_bbox` 字段 `[x1, y1, x2, y2]`
**新建文件**:
- `libs/subject/__init__.py`
- `libs/subject/detector.py` — 封装 YOLO 检测器（~80 行）
  ```python
  class SubjectDetector:
      def __init__(self, model_name='yolov8n.pt', device='cuda'):
          self.model = YOLO(model_name)
      def detect(self, frame: np.ndarray) -> list[dict]:
          """返回人物检测框列表 [{bbox, confidence}, ...]"""
      def detect_at_timestamp(self, video_path, timestamp) -> dict:
          """在指定时间戳检测人物，返回最高置信度框"""
  ```
- `libs/subject/tracker.py` — 封装跟踪器（阶段 2，~100 行）
**修改文件**:
- `eval.py` — 添加 `--with-subject` 参数，推理后运行主体关联
- `visualize.py` — 可视化中叠加检测框
**验证方法**: 选 5 个 THUMOS14 体育类视频（如篮球、排球、跳高），视频帧 + 检测框 + 动作时间轴叠加展示，人工判断关联是否合理。
**阶段 2: 多目标跟踪（时间允许时，1-2 天）**
引入跟踪算法，给每个主体分配稳定 ID，区分不同人物：
- **方案 A（推荐）: BoT-SORT / ByteTrack**
  - 基于 YOLO 检测结果 + Kalman 滤波 + 匈牙利匹配
  - 使用 `boxmot` 库或直接从 YOLOv8 内置 `model.track()` 调用
  - 优势: 实现简单（YOLOv8 自带 `.track()` 方法），跟踪 ID 稳定
- **方案 B: DeepSORT**
  - 在 SORT 基础上增加外观特征（ReID），对遮挡更鲁棒
  - 但需要额外的 ReID 模型，复杂度更高
**跟踪输出格式**:
```python
{
    'action_segments': [
        {'start': 10.5, 'end': 15.2, 'label': 'BasketballDunk', 'score': 0.85,
         'subjects': [
             {'track_id': 1, 'bbox': [100, 200, 300, 500], 'frames': [100, 110, ...]},
             {'track_id': 2, 'bbox': [400, 150, 600, 450], 'frames': [105, 115, ...]}
         ]}
    ]
}
```

**阶段 3: 动作-主体精匹配（进阶，1-2 天）**
基础方案只关联了时间中间帧，对快速移动的动作不准确。改进：
- 在动作的起止帧分别检测，检查主体是否在两端都存在（排除误检）
- 利用跟踪 ID 判断同一动作是否由多个主体交替完成（如接力赛）
- 使用光流或运动显著性图判断哪个主体与动作最相关（IOU 匹配 + 运动量分数）
**评估指标**（自行定义，无需外部标注）:
- 主体关联成功率：随机采样 50 个动作实例，人工判断主体框是否合理
- 跟踪 ID 稳定性：同一主体在连续片段中 ID 保持一致的比例
**参考文献与资源**:
- YOLOv8 + Tracking: [Ultralytics 官方文档](https://docs.ultralytics.com/modes/track/)
- BoT-SORT: Aharon et al., "BoT-SORT: Robust Associations Multi-Pedestrian Tracking", arXiv 2022
- ByteTrack: Zhang et al., "ByteTrack: Multi-Object Tracking by Associating Every Detection Box", ECCV 2022
- DeepSORT: Wojke et al., "Simple Online and Realtime Tracking with a Deep Association Metric", ICIP 2017
- MMAction2 中的时空动作检测 pipeline 可参考: https://github.com/open-mmlab/mmaction2
## 重叠动作与多主体动作检测
**核心问题**: 现有 TAD 方法（包括 TriDet）在设计时默认同一时刻最多只有一个动作发生。但真实视频中，多人场景里经常同时发生多个同类或不同类动作。TriDet 的 Trident-head 在设计上并没有处理多实例重叠的能力——同一时刻的分类头只预测一个类别，NMS 会抑制重叠度高的同类别预测。
### 问题分析
**做什么**: 分析 TriDet 在密集/重叠场景的表现，找到具体失效案例，量化问题的严重程度。
**分析脚本** (`analysis/error_analysis.py`):
- 统计每个视频的动作实例数量，按密度分层计算 recall
- 找出同类别时间接近（gap < 3s）的实例对，计算基线同时检出率
- 在 THUMOS14 验证集上生成 5-10 个典型失效案例的时间轴图
- 计算"重叠度"指标——对每个 GT 实例，计算其时间范围内 `该实例被检出 AND 同一时刻有多少其他实例也被正确检出` 的比率
- 按重叠度分桶（0%, 0-30%, 30-70%, >70%），看看 TriDet 在高度重叠场景下的 recall 掉多少
**现有参考**:
- **RefDense (arXiv 2025)**: 将密集动作检测拆解为"动作实体 + 动作运动"两个子概念，分别由不同的子网络检测，消除了语义重叠
- **DualDETR (CVPR 2024)**: 使用双层级（边界级+实例级）DETR 解码器，通过 query alignment 策略同时检测多个重叠动作，无需 NMS 后处理
- **MultiTHUMOS 数据集**: THUMOS14 的多标签版本，每个时刻标注了所有同时发生的动作，是测试重叠检测能力的事实标准
### 改进方向
按难度递增，建议至少完成方案 B，尝试方案 C
#### 方案A
Density-Aware NMS（推荐优先做，~30 行修改）
修改 `libs/utils/nms.py` 中的 `batched_nms`:
- 同类 NMS 阈值根据局部预测密度动态调整——预测框越密集的地方，阈值越宽松
- 或简单地将 soft-NMS 的 sigma 从 0.75 提高到 0.85，让同类多实例更容易共存
- 实现思路:
  ```python
  # 在 batched_nms 中，根据每个位置的预测密度调整 iou_threshold
  density = compute_local_density(segs)  # 统计每个 seg 附近的 seg 数量
  adaptive_thresh = iou_threshold * (1 + beta * density)  # 密度高处阈值更高（更宽松）
  ```
- 优势: 不影响训练，只改推理后处理；可以快速看到效果
- 预期效果: 多实例场景 recall 提升 5-10%，整体 mAP 微涨或不退化
#### 方案B
多类别 Soft-NMS + 得分校准（~50 行修改）
- 在 NMS 阶段，不仅抑制同类预测，也考虑不同类别间的小幅抑制（避免同一个 segment 被多类别重复检测）
- 对 soft-NMS 衰减后的得分做"密度惩罚"——如果一个预测段与多个同类别 GT 重叠，保留多个高得分预测而非只取最高
#### 方案C
分离式检测头（架构级修改，2-3 天）
- 参考 RefDense 的思路，为 TriDet 增加一个"重叠感知分支"：
  - 在 FPN 输出后增加一个简单的 `OverlapPredictor`（小型 MLP），预测每个时刻可能同时发生的动作数量
  - 在推理阶段，如果某位置预测重叠数 > 1，则在该位置保留 top-K 个类别预测（而非默认的 top-1）
- 修改 `ClsHead` 的推理逻辑: 原来每个位置取 argmax，改为取 top-K（K 来自 OverlapPredictor）
- 训练时只需加一个小的辅助损失（MSE 预测重叠数 vs GT 重叠数）
#### 方案D
多主体感知的动作检测，可以与
- 先检测并跟踪每个主体，然后对每个主体独立做动作检测
- 用主体位置作为空间先验，帮助区分不同主体的重叠动作
- 这需要修改模型结构来接收主体特征作为额外输入
**评估**: 为量化重叠动作的改进效果，建议在 THUMOS14 验证集上设计一个"重叠场景子集"——筛选出任意两实例 tIoU > 0.3 的视频段，单独计算这些场景下的 mAP 和 recall。
**参考文献**:
- RefDense: "Reframing Dense Action Detection", arXiv 2025
- DualDETR: "Dual DETRs for Multi-Label Temporal Action Detection", CVPR 2024
- ActionFormer (原始基线): Zhang et al., ECCV 2022
- MultiTHUMOS: Yeung et al., "Every Moment Counts: Dense Detailed Labeling of Actions in Complex Videos", IJCV 2018
## 端到端特征提取
**核心问题**: TriDet 基线使用**预提取的冻结特征**（I3D/TSP 特征由外部模型从视频中提取后保存为 `.npy`）。这带来两个问题：(1) 特征提取模型是在动作分类任务上训练的，与时序定位任务存在**任务偏差**；(2) 特征提取模型的参数在TAD训练中不可学习，无法针对定位任务优化。端到端训练可以缓解这些问题，但面临**显存瓶颈**——将长视频的原始帧送入3D CNN 会消耗巨大显存。
另外就是数据问题，THUMOS14的原始视频数据下载比较麻烦，也需要考虑办法解决。
**当前最新进展**（2024-2025）:
- **AdaTAD (CVPR 2024)**: 提出 Temporal-Informative Adapter (TIA)，在冻结的视频骨干（如 VideoMAE）中插入轻量可训练适配器，使 TAD 训练时只需更新适配器参数而非整个骨干。这是第一个端到端方法在 THUMOS14 上超越所有基于预提取特征的方法（75.4% mAP @ THUMOS14）。AdaTAD++ (ICCV 2025) 进一步通过时空分离适配器将性能提升至 78.0%。
- **LoFi (NeurIPS 2021)**: 降低预训练阶段的时空分辨率来进行端到端优化，使梯度可以从 TAD 损失回传到视频编码器
- **VideoMAE**: 自监督预训练的 Video Transformer，是AdaTAD使用的骨干网络
AdaTAD 需要 VideoMAEv2-giant (1B 参数) 级别的骨干和 1536 帧的视频输入，个人显卡跑起来比较困难，如果真需要我们可以去租卡。但至少我们可以借鉴其核心思想——**用小型化的适配器方案实现端到端的可行性验证**。
### 技术路线
#### 阶段 1: 特征替换验证
不涉及训练，先跑通整个流程，仅验证不同来源的特征可以插入 TriDet:
- 使用 torchvision 的预训练 S3D 或 X3D-S 模型，对 THUMOS14 的少量视频提取特征
- 将特征保存为 `.npy`，格式对齐现有 DataLoader 的约定 `(C, T)`
- 用新特征直接跑一次 TriDet 推理，确认维度兼容
- 对比 I3D 特征和新特征的 mAP 差异，分析原因
**新建文件**:
- `scripts/extract_features.py` — 通用特征提取脚本（~100 行）
  - 支持 I3D / S3D / X3D / VideoMAE 等多种骨干
  - 自动分 chunk 处理长视频（每段 N 帧），避免显存溢出
  - 输出 `.npy` 到指定目录，记录 feat_stride 和 feat_dim
#### 阶段 2: 轻量端到端训练
借鉴 AdaTAD 的 TIA 思想，在预训练骨干中插入小型可训练模块：
- **骨干选择**: 使用较小的预训练模型作为起点（VideoMAE-Base 或 SlowFast-R50），而非 1B 参数的 ViT-g
- **适配器设计**（简化版 TIA）:
  ```python
  class LightweightTIA(nn.Module):
      """轻量级时序信息适配器，插入到冻结骨干的每层输出后"""
      def __init__(self, dim, reduction=4):
          # 时序卷积 + 通道门控，参数量仅几千
          self.temporal_conv = nn.Conv1d(dim, dim, 3, padding=1, groups=dim)  # 深度可分离
          self.gate = nn.Sequential(
              nn.AdaptiveAvgPool1d(1),
              nn.Conv1d(dim, dim // reduction, 1),
              nn.ReLU(),
              nn.Conv1d(dim // reduction, dim, 1),
              nn.Sigmoid()
          )
      def forward(self, x):
          return x + self.gate(x) * self.temporal_conv(x)
  ```
- **训练策略**:
  - 冻结预训练骨干的所有参数
  - 只训练 TIA 适配器 + TriDet 检测头（FPN + ClsHead + RegHead）
  - 输入: 原始视频帧（而非预提取特征），分 chunk 送入骨干
  - 梯度检查点 (gradient checkpointing) 降低显存，每 chunk 处理 32-64 帧
  - 目标: 在 16GB 显存下完成端到端训练
**新建/修改文件**:
- `libs/modeling/e2e_adapter.py` — TIA 适配器实现（~60 行）
- `libs/modeling/e2e_backbone.py` — 封装"冻结骨干 + 适配器"的端到端特征提取器（~100 行）
- `configs/thumos_e2e.yaml` — 端到端训练配置（降低 batch_size，启用混合精度）
- `train_e2e.py` 或修改 `train.py` — 支持端到端模式（原始视频输入 → 骨干 → TriDet）
**显存优化技巧**:
- 使用 `torch.cuda.amp` 混合精度训练（FP16）
- 梯度累积 (gradient_accumulation_steps=4) 用小 batch 模拟大 batch
- 分 chunk 处理: 将 768 帧的视频切成 12 段 × 64 帧，逐段前向，拼接特征后再送入 TriDet
- 如果仍然超显存，进一步缩短输入帧数（512 帧，降低 max_seq_len 到 256）
- 使用 `torch.utils.checkpoint` 在骨干的 transformer 层上做梯度检查点
**验证目标**:
- 端到端训练能跑通，不 OOM
- 在 THUMOS14 上达到至少接近基线的 mAP（即便低于 68.59% 也可以接受，重点是证明流程可行）
- 如果没有达到基线，分析原因（是骨干太小？适配器容量不够？还是训练不充分？）

#### 阶段 3: 完整端到端
如果时间允许
- 换用更大的预训练骨干（VideoMAE-Base, 在 Kinetics-400 或 Something-Something v2 上预训练）
- 用 LoFi 的低保真预训练策略（在降采样视频上预训练适配器，再微调）
- 对比 "冻结骨干+适配器" vs "全量微调骨干最后几层" 的性能差异
- 在 ActivityNet 上验证端到端方案的泛化性
- 或者干脆一点租卡去

**参考文献**:
- AdaTAD: Liu et al., "End-to-End Temporal Action Detection with 1B Parameters Across 1000 Frames", CVPR 2024
- AdaTAD++: Agrawal et al., "Scaling Action Detection: AdaTAD with Transformer-Enhanced Temporal-Spatial Adaptation", ICCV 2025
- LoFi: Xu et al., "Low-Fidelity End-to-End Video Encoder Pre-training for Temporal Action Localization", NeurIPS 2021
- VideoMAE: Tong et al., "VideoMAE: Masked Autoencoders are Data-Efficient Learners for Self-Supervised Video Pre-Training", NeurIPS 2022
- SlowFast: Feichtenhofer et al., "SlowFast Networks for Video Recognition", ICCV 2019
- 端到端 TAD 综述: https://github.com/zhenyingfang/Awesome-Temporal-Action-Detection-Temporal-Action-Proposal-Generation

---

# 整体改进规划

## 改进 1: 通道注意力模块 — SE/ECA Block（难度低，收益中等）
**思路**: 在 SGP 骨干的特征提取路径中插入轻量通道注意力，让网络自动学习不同通道的重要性权重。
**为什么选这个**:
- TriDet 的 SGPBlock 有五条分支 (`psi, fc, convw, convkw, global_fc`)，它们的输出直接相加。但不同分支对不同类型动作的贡献可能不同，通道注意力可以自动学习这个权重分配
- SE Block 实现极其简单（~15 行代码），参数量可忽略（`2 * C^2 / reduction`）
- 不改变特征尺寸，不影响下游所有模块
**实现位置**: `libs/modeling/blocks.py`
```python
class SELayer(nn.Module):
    """Squeeze-and-Excitation 通道注意力（1D版）"""
    def __init__(self, channels, reduction=16):
        super().__init__()
        self.fc = nn.Sequential(
            nn.AdaptiveAvgPool1d(1),
            nn.Conv1d(channels, channels // reduction, 1),
            nn.ReLU(inplace=True),
            nn.Conv1d(channels // reduction, channels, 1),
            nn.Sigmoid()
        )

    def forward(self, x):
        return x * self.fc(x)
```

**插入位置**（三种选择，从保守到激进）:
1. **SGPBlock 的 FFN 之后**（最保守，仅重新分配 MLP 输出的通道权重）
2. **SGPBlock 的五分支融合之后、FFN 之前**（中等，重新分配多尺度融合特征的通道权重）
3. **Backbone 每个 stage 的输出之后**（最激进，在 FPN 融合前重新分配各层特征的通道权重）
建议同时测试三种插入位置，选效果最好的。
**备选**: ECA-Net (ECA-Net: Efficient Channel Attention) — 用 1D 卷积替代 SE 中的全连接，参数量更少：
```python
class ECALayer(nn.Module):
    def __init__(self, kernel_size=3):
        super().__init__()
        self.conv = nn.Conv1d(1, 1, kernel_size, padding=kernel_size//2, bias=False)
    def forward(self, x):
        avg = x.mean(dim=-1, keepdim=True)  # (B, C, 1)
        att = self.conv(avg.transpose(1, 2)).sigmoid().transpose(1, 2)
        return x * att
```

**预期效果**: mAP +0.5~1.5%（THUMOS14），参数增量 < 0.1%
## 改进 2: SGP 层超参优化（难度中低，收益中等）
**思路**: 原始 TriDet 在 THUMOS14 上使用 `k=5.0` 和 `sgp_win_size=[1]*6`。但论文中的消融实验只做了有限搜索。可以系统性地搜索更优的超参组合。
**搜索空间**:
- `k` 参数: 控制大窗口分支的卷积核大小放大倍数，搜索 `[1.0, 1.5, 3.0, 5.0, 7.0, 9.0]`
- `sgp_win_size`: 各层的局部窗口大小，尝试不对称配置 如 `[1,1,3,5,7,9]` 或 `[1,3,3,5,5,7]`
- `init_conv_vars`: SGP 卷积权重的初始化方差，搜索 `[0.1, 0.5, 1.0, 2.0]`
**执行**: 写一个 `scripts/hparam_search.py`，自动遍历超参组合，每轮训练 20 epoch 后在 THUMOS14 上评估，汇总到 CSV。
**预期效果**: 如果找到更好的组合，mAP +0.5~2%
## 改进 3: 特征金字塔增强 — BiFPN 风格连接（难度中，收益中高）
**思路**: 当前 FPN 只有自上而下的单向连接。BiFPN (EfficientDet, Tan et al. 2020) 引入了双向路径 + 跨层跳跃连接 + 可学习特征权重，信息流动更充分。
**简化实现**: 在现有 `FPN1D` 的基础上增加一条自下而上的路径：
```
原始 FPN:    P6 → P5 → P4 → P3 → P2 → P1 (只有 top-down)
增强后 FPN:  P6 ⇄ P5 ⇄ P4 ⇄ P3 ⇄ P2 ⇄ P1 (top-down + bottom-up)
```
**修改文件**: `libs/modeling/necks.py`，新增 `@register_neck("bifpn")` 的 `BiFPN1D` 类
**参考**: EfficientDet (CVPR 2020) 中的 BiFPN 设计，尤其注意其快速归一化融合方式：
```python
# 可学习权重融合（替代简单的相加）
w1 = torch.sigmoid(self.w1)  # 可学习标量
w2 = torch.sigmoid(self.w2)
fused = (w1 * feat1 + w2 * feat2) / (w1 + w2 + 1e-8)
```
**预期效果**: mAP +1~3%
## 改进 4: 回归损失函数增强（难度中低，收益中）
**思路**: 当前使用 DIoU 回归损失。可以尝试更先进的边界回归损失：
- **CIoU Loss**: 在 DIoU 的基础上增加长宽比（对 1D 场景是时长尺度）的一致性约束。在 1D 动作检测中等价于 DIoU，所以可能没额外收益。
- **EIOU Loss (Extended IoU, 2022)**: 将损失分解为 IoU 损失 + 中心距离损失 + 宽度损失三个正交项。对 TriDet 的 (left_offset, right_offset) 参数化有更好的建模。
- **Alpha-IoU (2021)**: 引入 power 参数对 IoU 做指数变换，可以通过调整 α 来灵活平衡不同质量预测框的梯度
- **Focaler-IoU (2024)**: 专门解决"IoU 分布的边缘值梯度消失"问题，通过分段函数对不同 IoU 区间的样本施加差异化的梯度
**实现**: 在 `libs/modeling/losses.py` 中新增损失函数，通过配置切换。做对比消融实验。
**建议**: 先做 EIOU（改动最小、理论更合理），如果效果明显再做 Focaler-IoU。
## 改进 5: 标签分配策略优化（难度中，收益中）
**思路**: TriDet 使用基于中心点采样 + 最短时长匹配的标签分配策略。可以尝试：
- **SimOTA** (YOLOX 的动态标签分配): 根据预测质量动态决定每个 GT 分配多少个正样本 anchor，而非固定半径
- **TaskAlignedAssigner** (YOLOv8 的分配器): 结合分类得分和回归质量（IoU）的加权分数来选择正样本
- **自适应中心采样半径**: 让 `center_sample_radius` 不是固定值，而是根据动作时长动态调整——长动作用大半径（更多正样本），短动作用小半径（避免背景污染）
**实现位置**: 修改 `meta_archs.py` 中的 `label_points_single_video` 方法
## 改进 6: 训练策略优化（难度低，收益低-中）
这些小改进不需要改模型结构，只改训练配置：

- **EMA 参数调优**: 当前 `decay=0.999`，可以尝试 `0.9999`(更平滑) 或 `0.99`(更快速适应)
- **混合精度训练**: 默认可能未启用 AMP，确认开启后可提升 ~30% 训练速度
- **Label Smoothing 搜索**: 当前默认 0.1，搜索 `[0.0, 0.05, 0.1, 0.15, 0.2]`
- **Gradient Clip 搜索**: 当前 `clip_grad_l2norm=-1`(不裁剪)，尝试 `1.0, 5.0, 10.0`
- **Warmup 策略**: 当前 warmup 5 epoch，测试 0/3/7/10 epoch 的区别
## 实验管理规范
为了让你能有效管理多个实验，建议建立统一的实验记录：
**新建文件**: `experiments.md`（或使用 W&B / TensorBoard）

```
| 实验ID | 日期 | 改进项 | 配置变更 | THUMOS mAP | ActivityNet mAP | 备注 |
|--------|------|--------|---------|-----------|-----------------|------|
| E01 | - | 基线 | - | 68.59% | 36.54% | Phase1 复现 |
| E02 | - | +SE Block | se_position=after_ffn, reduction=16 | xx% | xx% | - |
| ... | - | - | - | - | - | - |
```

## 最终集成
在所有子方向完成后，你的职责是将所有有效改进**整合到一个统一的 demo 流程**中：
1. 更新 `configs/` 下的配置文件，使所有改进可通过配置开关控制
2. 更新 `demo.py`（或新建），支持：
   - 选择模型配置（基线 / +SE / +BiFPN / 最佳组合）
   - 选择推理选项（--with-subject, --density-aware-nms, --overlap-thresh）
   - 一键输出可视化结果 + 评估指标
1. 生成一份综合实验结果对比表（基线 + 各改进 + 最佳组合），用于答辩
## 参考文献
- SE-Net: Hu et al., "Squeeze-and-Excitation Networks", CVPR 2018
- ECA-Net: Wang et al., "ECA-Net: Efficient Channel Attention for Deep Convolutional Neural Networks", CVPR 2020
- CBAM: Woo et al., "CBAM: Convolutional Block Attention Module", ECCV 2018
- BiFPN / EfficientDet: Tan et al., "EfficientDet: Scalable and Efficient Object Detection", CVPR 2020
- EIoU: Zhang et al., "Focal and Efficient IOU Loss for Accurate Bounding Box Regression", Neurocomputing 2022
- Alpha-IoU: He et al., "Alpha-IoU: A Family of Power Intersection over Union Losses for Bounding Box Regression", NeurIPS 2021
- SimOTA / YOLOX: Ge et al., "YOLOX: Exceeding YOLO Series in 2021", arXiv 2021
- FDDet (频率解耦+边界细化): "FDDet: Frequency-Decoupling for Boundary Refinement in Temporal Action Detection", arXiv 2025
